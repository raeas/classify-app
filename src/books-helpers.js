// let arr = []
//   categories.forEach((category, index) => {
//     subcategories.forEach(sub => {
//       if(category.hasOwnProperty('subcategories') && category['id'] === sub['category']) {
//         arr[index]['subcategories'].push(sub)
//       } 
//       if(!category.hasOwnProperty('subcategories') && category['id'] === sub['category']) {
//         category['subcategories'] = [sub]
//         arr.push(category)
//       }
//     })
//   })

//   export const findCatsAndSubcats = 